package org.example.DSA.Array;

import java.util.Arrays;

public class Array_Problems {


    public static void main(String[] args) {



        int[] rollNo;  // declaration of array, rollNo is defined in the stack.
        rollNo = new int[5]; //initialisation: here object is being created in the memory (heap)
        System.out.println(rollNo[0]); // for the type integer array have byDefault value 0;
        // and for type string we get byDefault null value.


        // new is used to create an object so it will create the object of type array...size 5 in the heap memory.
        // in c++ array elements are in continuous blocks of memory, but in java array objects are in heap and heap
        // objects are not continuous it depends on the JVM.



        // Array of Objects

    }
}
